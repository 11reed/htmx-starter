<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/htmx.org@2.0.1" integrity="sha384-QWGpdj554B4ETpJJC9z+ZHJcA/i59TyjxEPXiiUgN2WmTyV5OEZWCD6gQhgkdpB/" crossorigin="anonymous"></script>
    <link href="/src/output.css" type="text/css">
    <title>My TypeScript App</title>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold text-red-500">Posts</h1>
        <ul>
            {% for post in posts %}
                <li>{{ post.title }}</li>
            {% else %}
                <li>No posts yet</li>
            {% endfor %}
        </ul>
        <form hx-post="/create_post" hx-swap="outerHTML">
            <input type="text" name="title" placeholder="Title" class="border p-2">
            <textarea name="content" placeholder="Content" class="border p-2"></textarea>
            <button type="submit" class="bg-blue-500 text-white p-2">Create Post</button>
        </form>
    </div>
</body>
</html>
